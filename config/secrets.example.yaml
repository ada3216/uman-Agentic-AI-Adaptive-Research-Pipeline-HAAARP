# EXAMPLE ONLY — copy to config/secrets.yaml and fill in real values
# config/secrets.yaml is gitignored and must never be committed

osf:
  token: YOUR_OSF_PERSONAL_ACCESS_TOKEN_HERE
  # Get your token at: https://osf.io/settings/tokens/

# If using an alternative local model requiring auth:
# model:
#   api_key: YOUR_KEY_HERE
